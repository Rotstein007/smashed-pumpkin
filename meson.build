project('smashed-pumpkin', 'c',
  version: '0.5.1',
  meson_version: '>= 1.3.2',
  default_options: [
    'warning_level=2',
    'buildtype=debugoptimized'
  ]
)

gnome = import('gnome')

subdir('src')
subdir('data')
